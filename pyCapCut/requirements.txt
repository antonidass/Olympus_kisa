pycapcut>=0.0.3
# pymediainfo ставится автоматически вместе с pycapcut, но фиксируем явно.
pymediainfo>=7.0.0
# mutagen используется build_*.py для точного измерения длины mp3
# (счётчик MPEG-фреймов, не заголовок контейнера) — без него
# audio.duration в JSON может превышать реальный mp3 на 5–15 мс,
# что даёт «робовойс» на последнем слоге в CapCut. См. CAPCUT.md §7.1.
mutagen>=1.47.0
